<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update place - Bestpoints</title>
</head>

<body>
    <h1>Update Place</h1>
    <form action="/places/<%= place._id %>/update?_method=PUT" method="POST">
        <div class="form">
            <label for="title">Title</label>
            <input type="text" name="place[title]" id="title" value="<%= place.title %>">
        </div>
        <div class="form">
            <label for="price">Price</label>
            <input type="number" name="place[price]" id="price" value="<%= place.price %>">
        </div>
        <div class="form">
            <label for="description">Description</label>
            <input type="text" name="place[description]" id="description" value="<%= place.description %>">
        </div>
        <div class="form">
            <label for="location">Location</label>
            <input type="text" name="place[location]" id="location" value="<%= place.location %>">
        </div>
        <button type="submit">Save</button>
    </form>
</body>

</html>